export function parseCoreSnapshot(reply) { const fields = String(reply).split('|'), revision = Number(fields[6]); if (fields[0] !== 'ok' || fields.length !== 7 || !fields[1] || !fields[2] || !['muscle', 'fascia'].includes(fields[3]) || !['true', 'false'].includes(fields[4]) || !['true', 'false'].includes(fields[5]) || !Number.isSafeInteger(revision) || revision < 0) return null; return Object.freeze({ region: fields[1], selected: fields[2], layer: fields[3], isolated: fields[4] === 'true', overview: fields[5] === 'true', revision }); }
export function focusPlanFromBounds(bounds) { const dx = bounds.max.x - bounds.min.x, dy = bounds.max.y - bounds.min.y, dz = bounds.max.z - bounds.min.z, radius = Math.max(Math.hypot(dx, dy, dz) / 2, .001); return { target: { x: (bounds.min.x + bounds.max.x) / 2, y: (bounds.min.y + bounds.max.y) / 2, z: (bounds.min.z + bounds.max.z) / 2 }, distance: radius * 3.1 }; }
