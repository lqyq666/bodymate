# MoonBit owns the complete-body motion domain

BodyMate keeps Motion Definitions, parameter validation, Participation Profiles, Motion Session transitions, and Pose Intents in MoonBit so the default complete-body workflow has one authoritative domain engine. JavaScript and Three.js consume versioned output and retain only Presentation responsibilities; this costs a deliberate serialization boundary but prevents the headline product path from becoming a separate JavaScript domain model.
