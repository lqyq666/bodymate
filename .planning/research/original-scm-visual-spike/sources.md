# Sources: Original SCM visual spike

## Repository evidence

- `prototype/real-neck/spike-app.mjs` — existing isolated renderer contract for Registry → MoonBit → snapshot → renderer.
- `prototype/real-neck/spike-domain.mjs` — snapshot parsing, isolate visibility, and bounds-focus plan convention.
- `docs/ANATOMY_ASSET_DECISION.md` — third-party asset provenance is a separate, reviewed concern and must not be silently mixed into this spike.

## Decision

No external geometry, metadata, screenshots, or source-derived measurements are used by this prototype. The result is an independently-authored visual and interaction experiment only.
