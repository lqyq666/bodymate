{
  "name": "bodymate",
  "version": "0.1.0",
  "private": true,
  "description": "Offline anatomy exploration whitebox",
  "scripts": {
    "build": "node scripts/build-moonbit.mjs",
    "verify": "node scripts/run-moon.mjs check --target js && node scripts/run-moon.mjs test --target js && node --test test/bodymate.contract.test.mjs",
    "check": "npm run build && npm run verify"
  }
}
